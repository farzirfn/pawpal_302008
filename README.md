# pawpal

This app integrates Flutter (front-end) with PHP and MySQL (back-end), focusing specifically on the login and registration page features. It allows users to register an account, validate their credentials, and securely log in using data stored in the MySQL database.

